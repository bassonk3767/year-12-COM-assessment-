
# calculates the median
# of the provided data set
def find_median(data_set):

    # Initialize the rough value
    # as the beginning of the calculation
    # ( the index for the median )
    index = int(float(len(data_set)) / 2)

